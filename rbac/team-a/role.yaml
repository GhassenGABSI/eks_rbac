apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: selogerproxy-digital-nonprod-rw
  namespace: app-one
rules:
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
